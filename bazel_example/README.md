# Bazel Example

In `MODULE.bazel` and `BUILD.bazel`, you can see how Portable Python can be used within Bazel.

Unfortunately, it seems to require shell scripting.
At least, avoiding shell makes it much more complicated.
Mostly the "untar" step is tricky.
